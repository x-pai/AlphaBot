请使用 ashare-midday-brief Skill，生成 {date} 的 A 股午盘快报。

目标：输出一份条件化午后操作计划，让读者明确今天是否参与、优先板块和候选股、参与与放弃条件、时间计划及风险上限。

以 Asia/Shanghai 时区当日 11:30 为行情截面，使用本次可用工具核验真实数据。先判断市场属于强势共振、结构性行情、震荡分化、高位分歧或情绪退潮，再给出与环境一致的操作倾向和计划总仓上限。

按照“午间决策 → 环境证据 → 板块优先级 → 个股候选池 → 午后执行与风险 → 最终清单”输出。板块最多一个 S 级、一个 A 级和一个 B 级；候选股最多三只，每只必须包含代码、角色、个股与板块共同触发条件以及对应放弃条件。

没有满足条件的 S 级板块时明确写“无 S 级主攻”，不生成参与型候选。个股行情、成交、日线或公告核验不完整时只能标为观察，不给买点。所有均线、价位、炸板率、连板和仓位数字必须有数据或风险参数依据。

半日量能仅与上一交易日同期比较；缺失数据不编造，没有同期基准不判放缩量。若午后补跑，不得混入下午或全天行情；午间新增消息单列，不倒推为上午行情原因。核心数据不足时收缩结论，非交易日说明休市。

全文约 700–1100 字。直接输出可发布的中文 Markdown 正文，不保留模板占位符，不输出工具调用过程，不自行发布、发送通知或执行交易。
